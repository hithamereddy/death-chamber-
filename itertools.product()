#!/usr/bin/python3

from __future__ import print_function
from itertools import product

all = tuple(product(map(int, input().strip().split(" ")), map(int, input().strip().split(" "))))

for i in all:
    print(i, end=" ")
